# Secure Implementation

This implementation safely extracts archives by calculating and normalizing the absolute path of every entry, ensuring it never escapes the intended directory. It satisfies ASVS 5.3.3.
