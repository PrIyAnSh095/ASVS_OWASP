version: '3.8'

services:
  secure-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app

networks:
  default:
    name: asvs-5.4.2-secure-net
