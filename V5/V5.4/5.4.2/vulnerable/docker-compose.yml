version: '3.8'

services:
  vulnerable-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app

networks:
  default:
    name: asvs-5.4.2-vulnerable-net
