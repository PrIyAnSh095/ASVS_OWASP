version: '3.8'

services:
  secure-app:
    build:
      context: ..
      dockerfile: secure/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development

networks:
  default:
    name: asvs-5.4.3-secure-net
