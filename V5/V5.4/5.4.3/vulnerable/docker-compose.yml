version: '3.8'

services:
  vulnerable-app:
    build:
      context: ..
      dockerfile: vulnerable/Dockerfile
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=development

networks:
  default:
    name: asvs-5.4.3-vulnerable-net
