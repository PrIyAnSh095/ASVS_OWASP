version: '3.8'
services:
  vulnerable:
    build:
      context: ..
      dockerfile: vulnerable/Dockerfile
    container_name: asvs415_vulnerable
    ports:
      - "5001:5000"
    restart: unless-stopped
